import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  portfolioButtons: {
    columnGap: 12,
    flexDirection: 'row',
    width: '100%',
  },

  portfolioContainer: {
    paddingHorizontal: 20,
    width: '100%',
  },

  text: {
    color: '#A0A0A0',
    fontFamily: 'Sora',
    fontSize: 13,
    marginBottom: 12,
    marginTop: 12,
  },
});
