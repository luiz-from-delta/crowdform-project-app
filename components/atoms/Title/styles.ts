import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  title: {
    color: '#000000',
    fontFamily: 'Sora',
    fontSize: 18,
    fontWeight: '600',
    marginVertical: 20,
    textAlign: 'center',
    width: '100%',
  },
});
