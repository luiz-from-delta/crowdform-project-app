import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  title: {
    color: '#000000',
    fontFamily: 'Sora',
    fontSize: 18,
    fontWeight: '600',
    marginHorizontal: 20,
    marginTop: 20,
    width: '100%',
  },
});
