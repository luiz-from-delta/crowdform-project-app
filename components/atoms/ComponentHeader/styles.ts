import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    columnGap: 8,
    flexDirection: 'row',
    marginHorizontal: 10,
    marginTop: 20,
    width: '100%',
  },

  title: {
    color: '#000000',
    fontFamily: 'Sora',
    fontSize: 18,
    fontWeight: '600',
  },
});
