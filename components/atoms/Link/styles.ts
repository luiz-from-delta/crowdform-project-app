import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  title: {
    color: '#770FDF',
    fontFamily: 'Sora',
    fontSize: 12,
    fontWeight: '600',
    marginVertical: 20,
    textAlign: 'center',
    width: '100%',
  },
});
